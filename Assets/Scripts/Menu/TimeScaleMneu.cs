using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//на любой объект в меню
public class TimeScaleMneu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Time.timeScale = 1;
    }
}
